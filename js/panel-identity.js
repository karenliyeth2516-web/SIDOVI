(async()=>{const el=document.getElementById('panelIdentity');if(!el)return;try{const r=await fetch('/api/auth/me');const d=await r.json();if(!r.ok)throw new Error(d.error);const u=d.usuario||{};const foto=u.foto_perfil?.startsWith('data:image')?`<img src="${u.foto_perfil}" alt="Foto de ${u.nombre_completo}">`:'';const title=document.getElementById('panelTitle');if(title)title.textContent=u.nombre_completo||title.textContent;el.innerHTML=`${foto}<span><strong>Bienvenido, ${u.rol==='Gerente'?'gerente':'equipo de RRHH'} ${u.nombre_completo||''}</strong><small>${u.correo||''}</small></span>`;}catch(e){el.textContent='Sesión no disponible';}})();
